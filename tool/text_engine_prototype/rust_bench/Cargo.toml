[package]
name = "rust_bench"
version = "0.0.0"
edition = "2024"
publish = false
description = "THROWAWAY: the liber_text engine alone, for ticket #19's benchmark (no Dart VM in the process)"

[dependencies]
liber_text = { path = "../../../packages/fjs/liber_text" }
encoding_rs = "0.8"

[profile.release]
debug = false

[workspace]
